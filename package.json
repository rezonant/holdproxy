{
  "name": "holdproxy",
  "version": "1.0.0",
  "description": "A simple Express proxy which holds requests when no backend is available",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && node ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "holdproxy": "./dist/main.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.2",
    "js-yaml": "^3.10.0",
    "rxjs": "^5.5.6",
    "source-map-support": "^0.5.3"
  },
  "devDependencies": {
    "@types/express": "^4.11.1",
    "@types/node": "^9.4.6",
    "razmin": "^0.1.0",
    "typescript": "^2.7.2"
  }
}
